export const environment = {
  apiUrl: 'http://localhost:3000/api',
  wsUrl: 'ws://localhost:3000/ws/events',
  /** Ethereum Sepolia (testnet) — usada por MetaMask y el faucet del dashboard. */
  chainId: 11155111,
  /** API estilo Etherscan compatible con Blockscout (lista de tx por cuenta). */
  sepoliaExplorerApiUrl: 'https://eth-sepolia.blockscout.com/api',
  /** Explorador web para abrir la dirección conectada. */
  sepoliaExplorerAddressUrl: 'https://eth-sepolia.blockscout.com/address/',
};